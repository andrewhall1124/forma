// Shared between the client helpers (lib/athlete-mode.ts) and the server
// resolver (lib/access.ts), which can't import each other's runtimes.
export const ATHLETE_COOKIE = "forma-athlete";
export const ATHLETE_NAME_COOKIE = "forma-athlete-name";
